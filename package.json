{
  "name": "cortexyoung",
  "version": "5.0.0",
  "private": true,
  "description": "Semantic code intelligence CLI — local embeddings + Turso cloud",
  "workspaces": ["cli"],
  "scripts": {
    "build": "npm run build --workspace=cli",
    "dev": "cd cli && npx tsx src/index.ts",
    "test": "npm run test:phase2-smoke --workspace=cli"
  }
}
