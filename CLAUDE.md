# CLAUDE.md

This repo is xgrep-centered. Use `xg` when `command -v xg` succeeds and the task matches `skills/xgrep/SKILL.md`; otherwise use native Grep (`rg`). If `xg` is missing, point the user to `./install.sh` — do not attempt to install automatically.
