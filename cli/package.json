{
  "name": "cortex-cli",
  "version": "5.0.0",
  "description": "Semantic code intelligence CLI — local embeddings + Turso cloud",
  "type": "module",
  "bin": {
    "cortex": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test:phase2-smoke": "npm run build && node tests/phase2-smoke.mjs"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "@xenova/transformers": "^2.17.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
