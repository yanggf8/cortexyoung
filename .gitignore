# Keep until manual cleanup of archived tree (v6-final)
node_modules/
dist/
build/
*.tsbuildinfo

# Machine-local Claude Code settings and generated reports — never commit
.claude/settings.local.json
CORTEX_REPORT.md

# OS / editor (minimal)
.DS_Store
*.swp
*.swo
